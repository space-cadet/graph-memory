{
  "name": "graph-memory",
  "version": "1.0.0",
  "description": "Knowledge graph with semantic embeddings for agent memory",
  "main": "scripts/embeddings.cjs",
  "scripts": {
    "embed": "node scripts/embeddings.cjs",
    "test:embed": "node scripts/embeddings.cjs --text \"test query\""
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "better-sqlite3": "^11.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
