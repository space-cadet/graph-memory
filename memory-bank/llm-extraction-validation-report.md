# LLM vs Regex Extraction Comparison Report

**Date:** 2026-06-24
**Task:** workspace-37a — Test and validate LLM-based extraction
**Method:** Ran `compare-extraction.cjs` on 4 content-rich session files (3.7KB–19.5KB text)

## Summary

| Session | Text Size | Regex Entities | LLM Entities | Overlap | Regex-only | LLM-only |
|---------|-----------|----------------|--------------|---------|------------|----------|
| crash-monitor (small) | 3.7KB | 2 | 6 | 1 | 1 | 5 |
| heartbeat (medium) | 8.7KB | 6 | 9 | 4 | 2 | 5 |
| crash-monitor (medium) | ~8KB | 2 | 7 | 1 | 1 | 6 |
| science-news (large) | 19.5KB | 4 | 12 | 0 | 4 | 12 |
| **Total** | **~40KB** | **14** | **34** | **6** | **8** | **28** |

**Key Finding:** LLM extraction consistently finds 2–3× more entities than regex-only. On the science-news session, there was **zero overlap** — the two methods extract entirely different types of information.

## What Regex Catches Well

- **File names:** `README.md`, `manifest.json`, `crash-recovery.sh`
- **Tool references:** `git`, `openclaw`, `node`
- **Task references:** `T53`, `T34` (via `\b(T\d{1,3})\b` pattern)
- **arXiv IDs:** `arXiv:2401.12345`
- **GitHub repos:** `github.com/owner/repo`
- **Explicit wiki links:** `[[Page Name]]`

## What LLM Catches That Regex Misses

- **Domain concepts:** "Quantum Ghost Imaging", "room-temperature quantum materials", "String theory", "Quantum gravity"
- **People:** "Deepak" (identified as physics PhD)
- **Research papers:** "AI Reveals the Invisible Magnetic Chaos..."
- **Decisions:** 4 decisions found in the science-news session (regex: 0)
- **Topics:** 5 topics found in the science-news session (regex: 0)
- **Projects/tools in context:** "openclaw" identified as a tool, "Moltbook" as a project
- **Semantic descriptions:** LLM provides 10–20 word descriptions for each entity

## Entity Quality Comparison

### Regex
- **Confidence:** Fixed at 0.6–0.7 (arbitrary)
- **Descriptions:** Generic pattern name (e.g., "Detected via regex pattern: file")
- **Types:** Limited to 6 hardcoded categories (file, tool, project, research_paper, error, task)
- **Context:** Raw match string only

### LLM
- **Confidence:** 0.5–1.0 (contextually appropriate)
- **Descriptions:** Semantic, human-readable (e.g., "A technique using quantum mechanics for imaging")
- **Types:** 9 categories (person, project, file, tool, concept, institution, research_paper, task, error, decision)
- **Context:** Verbatim sentence from the source text

## Observed Issues

1. **No overlap on science-news session:** Regex extracts technical artifacts; LLM extracts domain concepts. This is actually **complementary** — they capture different dimensions of the conversation.

2. **Regex false positives:** `heartbeat` matched as a project name from `workspace/HEARTBEAT` path pattern. `t53` matched as a task from arbitrary text containing "T53".

3. **LLM entity merging:** Some entities appear with slightly different names across chunks (e.g., `memory/crash-monitor-state.json` vs `crash-monitor-state.json`). The deduplication by lowercase name handles this.

4. **LLM type assignment:** `Phys.org` and `ScienceDaily` were typed as `file` instead of `institution` or `website`. The LLM prompt could be refined to better distinguish content sources.

## Confidence Score Distribution

| Method | Avg Confidence | Range |
|--------|---------------|-------|
| Regex | 0.68 | 0.6–0.7 (fixed) |
| LLM | 0.92 | 0.5–1.0 (contextual) |

## Recommendations

1. **Keep both extractors.** They are complementary. Regex is fast and reliable for technical artifacts. LLM is slower but captures semantic richness.

2. **Use LLM as primary, regex as fallback.** The current `session-entity-extractor.cjs` already does this — LLM entities are merged first, then regex supplements.

3. **Consider fine-tuning the LLM prompt** to better categorize websites/institutions and reduce false positives.

4. **Monitor cost.** LLM extraction uses API calls (~1 call per 8K chars). For a typical 20KB session, that's 2–3 calls. At OpenRouter rates, this is negligible.

5. **Schema is ready.** The database already supports `confidence`, `description`, and `context` columns. No schema changes needed.

## Verdict

**LLM extraction is a significant improvement over regex-only.** It finds 2–3× more entities, captures decisions/topics that regex misses entirely, and provides richer metadata (descriptions, contextual confidence). The current hybrid approach (LLM primary + regex fallback) is working well and should be the default for production builds.

---
*Generated by beads executor (auto) — 2026-06-24*
